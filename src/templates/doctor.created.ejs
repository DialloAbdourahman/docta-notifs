<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome to DOCTA – Activate Your Account</title>
    <style>
      body {
        font-family:
          -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu,
          Cantarell, "Fira Sans", "Droid Sans", "Helvetica Neue", Arial,
          sans-serif;
        background: #f6f9fc;
        margin: 0;
        padding: 0;
        color: #222;
      }
      .container {
        max-width: 560px;
        margin: 24px auto;
        background: #ffffff;
        border-radius: 10px;
        border: 1px solid #e5e7eb;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
        overflow: hidden;
      }
      .header {
        background: #0f62fe;
        color: #ffffff;
        padding: 20px 24px;
        font-size: 18px;
        font-weight: 600;
      }
      .content {
        padding: 24px;
        line-height: 1.6;
      }
      .greeting {
        font-size: 16px;
        margin: 0 0 12px;
      }
      .paragraph {
        margin: 0 0 16px;
      }
      .button {
        display: inline-block;
        padding: 12px 18px;
        background: #0f62fe;
        color: #ffffff !important;
        text-decoration: none;
        border-radius: 6px;
        font-weight: 600;
      }
      .muted {
        color: #6b7280;
        font-size: 13px;
      }
      .footer {
        padding: 16px 24px;
        color: #6b7280;
        font-size: 12px;
        background: #fafbfc;
      }
      .link {
        color: #0f62fe;
        word-break: break-all;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">Welcome to DOCTA</div>
      <div class="content">
        <p class="greeting">Dear Dr. <%= fullName %>,</p>
        <p class="paragraph">
          We’re excited to have you join our network of trusted medical
          professionals on <strong>DOCTA</strong>.
        </p>
        <p class="paragraph">
          An administrator has created your account. Please activate it by
          clicking the button below to set your credentials and access your
          doctor dashboard.
        </p>
        <p>
          <a
            class="button"
            href="<%= activationUrl %>"
            target="_blank"
            rel="noopener noreferrer"
          >
            Activate and Log In
          </a>
        </p>
        <p class="muted">
          For security reasons, this link will expire after a short time. If it
          does, you can request a new activation email from your administrator.
        </p>
      </div>
      <div class="footer">
        If you did not expect this account creation, you can safely ignore this
        email.
      </div>
    </div>
  </body>
</html>
